@model IEnumerable<LuanVan.Models.CHITIETNHAP>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PHIEUNHAPSP.NV_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SANPHAM.NSP_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CTN_GIA)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PHIEUNHAPSP.NV_ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SANPHAM.NSP_ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CTN_GIA)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CTN_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CTN_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CTN_ID })
        </td>
    </tr>
}

</table>
