@model IEnumerable<LuanVan.Models.TRANGTHAIBH>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Danh sách</h2>

<p>
    @Html.ActionLink("Thêm trạng thái mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TTBH_TEN)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TTBH_MOTA)
        </th>
        <th>Tùy chọn</th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TTBH_TEN)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TTBH_MOTA)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.TTBH_ID }) |
            @Html.ActionLink("Chi tiết", "Details", new { id=item.TTBH_ID }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.TTBH_ID })
        </td>
    </tr>
}

</table>
