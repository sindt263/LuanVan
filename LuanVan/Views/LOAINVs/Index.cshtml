@model IEnumerable<LuanVan.Models.LOAINV>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LNV_TEN)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LNV_MOTA)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LNV_TEN)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LNV_MOTA)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LNV_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LNV_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LNV_ID })
        </td>
    </tr>
}

</table>
