@model IEnumerable<LuanVan.Models.LOAINV>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Danh sách</h2>

<p>
    @Html.ActionLink("Thêm quyền mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LNV_TEN)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LNV_MOTA)
        </th>
        <th>Tùy chọn</th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LNV_TEN)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LNV_MOTA)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.LNV_ID }) |
            @Html.ActionLink("Chi tiết", "Details", new { id=item.LNV_ID }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.LNV_ID })
        </td>
    </tr>
}

</table>
