@model IEnumerable<LuanVan.Models.GIASP>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Danh sách</h2>

<p>
    @Html.ActionLink("thêm giá mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GIA_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GIA_GIA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GIA_NGAYCAPNHAT)
        </th>
        <th>tùy chọn</th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GIA_ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GIA_GIA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GIA_NGAYCAPNHAT)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.GIA_ID }) |
            @Html.ActionLink("Chi tiết", "Details", new { id=item.GIA_ID }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.GIA_ID })
        </td>
    </tr>
}

</table>
