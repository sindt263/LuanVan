@model IEnumerable<LuanVan.Models.GIASP>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GIA_GIA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GIA_NGAYCAPNHAT)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GIA_GIA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GIA_NGAYCAPNHAT)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GIA_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.GIA_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GIA_ID })
        </td>
    </tr>
}

</table>
